'use strict';

define (
  function () {
    return function ($q) {
      return {
        require: 'ngModel',
        restrict: 'A',
        link: function($scope, iElem, iAttr, ctrl) {

        }
      };
    };
  }
);
