'use strict';

define (
  ['app'],
  function(app) {
    app.registerController('SearchController', [ '$scope', function($scope) {
      /**
       * Code for search box will go here.
       */
    }]);
  }
);